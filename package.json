{
  "name": "wladfa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Y Wladfa — La Huella de los Rifleros",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "build:single": "tsc --noEmit && vite build --mode single",
    "preview": "vite preview",
    "test": "vitest run",
    "validate:data": "node scripts/validate-data.mjs"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0",
    "vite": "^6.4.3",
    "vite-plugin-singlefile": "^2.0.3",
    "vitest": "^3.2.7"
  }
}
